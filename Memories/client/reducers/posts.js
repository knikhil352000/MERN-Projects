
const reducer = (posts = [], action) => {
    switch(acton.type) {
        case 'FETCH_ALL':
            ...posts,
            return posts;
        case 'CREATE' :
            return posts;
        default:
            return posts
    }
}

export default reducer
